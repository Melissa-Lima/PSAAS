#include <iostream>

using namespace std;

class Project {
    public:
    // aqui eu quero puxar um vetor de requisitos do arquivo recquirementsPhase.cpp. Não sei exatamente como fazer essa iteração...
        void requisitesInteration() {
            for(int i=0; i < requisitos.length(); i++) {

            }
        }

    // depois criar uma função que printa as subfases da fase de projeto.
};